<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Son Seçilen Otel</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
  <h2 class="mb-4 text-center">🛒 Sepetim</h2>

  <% if (!otel) { %>
    <div class="alert alert-warning text-center">
      Sepetiniz boş
    </div>
  <% } else { %>
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card shadow-lg">
          <img src="<%= otel.resim_url || 'https://via.placeholder.com/500x300' %>" class="card-img-top">

          <div class="card-body">
            <h3 class="card-title fw-bold"><%= otel.isim %></h3>

            <p class="card-text"><strong>Şehir:</strong> <%= otel.sehir %></p>
            <p class="card-text fs-4 text-success">
              <strong>Fiyat:</strong> ₺<%= otel.fiyat %>
            </p>

            <div class="d-flex justify-content-between mt-4">
              <a href="/oteller" class="btn btn-outline-primary btn-lg">
                Otellere Dön
              </a>
              <a href="/odeme" class="btn btn-success btn-lg">
                Ödemeye Geç
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% } %>
</div>
</body>
</html>